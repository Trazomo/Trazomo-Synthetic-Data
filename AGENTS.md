# Agent Orientation

This repository is the canonical synthetic-data universe for Trazomo training and exemplar repos. It is designed to be read by coding agents.

## Ground rules

- All data is fictional and safe to use in demos, tests, and templates.
- Entity IDs are canonical across the whole repo: the same company ID refers to the same fictional company in every dataset, in every track directory.
- Never invent additional facts about canon entities. Extend the universe by adding to `canon/` and regenerating via `datagen/`.
- Datasets contain deliberately planted anomalies for training exercises. Do not "fix" them; finding them is the exercise. Answer keys are intentionally not in this repo.
- Some datasets ship a `variants/` directory: a trimmed slice of the same data, derived from a sibling file by the rule recorded in `MANIFEST.json`. Read the rule before using a variant, and never edit one by hand; regenerate its parent.

## Layout

- `canon/` — companies, people, timeline (source of truth for IDs)
- `datasets/<track>/` — `legal`, `finance`, `hr`, `revenue`, `operations`, `smb`
- `datagen/` — deterministic, seeded generators (MIT)
- `MANIFEST.json` — dataset index: path, schema, row counts, version

## Current state

v1.7.0: the legal slice, the complete finance build through clusters 3 and 4 (35 finance datasets), the core datasets including the CORE-03 inbound lead-form view (additive; the five pre-existing CORE-03 CSVs byte-identical to v1.5.0), REV-07 crm-object-model-seed (the first `datasets/revenue/` entry), and 19 drafted artifact sets including FIN-21, FIN-28, FIN-30 and the operations capture spine (OPS-01 delivery sync transcript, OPS-02 retro transcript, OPS-03B follow-up email thread), which adds no `datasets/operations/` directory. The people and HR governance spine takes the drafted set to 22 (HR-01 role requisition library, HR-03 interview transcript corpus, HR-09 manager notes and review draft pair) and, in the deterministic half stacked on it, opens `datasets/hr/` with HR-17 mixed-sensitivity-employee-dataset and HR-18 hris-export, taking the dataset count to 47. HR-18's generator reads the frozen HR-01 register at build time, which is why the drafted half lands first. Both ship in the tag cut at their merge, expected v1.8.0. Operations cluster 2 (planning and intake) births `datasets/operations/` with OPS-04 intake-request-batch (request CSV, the published intake-rubric.yaml, six eml messages) and OPS-05 backlog-export-with-quality-gaps, and adds the drafted OPS-03 project brief, which joins the already-frozen CORE-01 master services agreement; it ships in the tag cut at its own merge, v1.9.0. The revenue cluster 2 slice (REV-01 consent-suppression-master with its policy JSON and two tool exports, the drafted REV-06 product-security fact sheet and claim snippets, REV-11 policy-as-code-scenarios) takes the pack to 51 datasets and 24 drafted sets in the tag cut at its own merge, v1.10.0; that PR changes no byte under `datasets/core/` or `canon/`. The revenue cluster 3 slice (REV-03 signal-event-logs, a 92-event jsonl signal log derived from CORE-03 at build time, and the drafted REV-04 message-template library with its 24-row recipient email-verification sidecar) takes the pack to 52 datasets and 25 drafted sets in the tag cut at its own merge, expected v1.11.0; that PR changes no byte under `datasets/core/`, `canon/`, the frozen cluster-2 revenue directories or `artifacts/REV-06/`, and its U-T3 correction (T3-prime: the champion's won relationship is the prior account's customer status plus the event's employment-history payload, because zero Closed Won opportunities exist in CORE-03) rides to the freeze review. Operations cluster 3 (risk and dependencies) adds OPS-06 raid-log-seed, OPS-07 work-item-graph-with-hidden-link (a work-item json plus a chat text corpus) and OPS-13 cross-functional-handoff-log to `datasets/operations/`, taking the pack to 55 datasets against the v1.11.0 baseline, and lands the approved R3 catalog edits as spec text only (OPS-15 consumer and format note, OPS-16 extension, the new OPS-17 entry); it ships in the tag cut at its own merge. People and HR cluster 2 amendment A moves the recruiting arc onto an operations requisition: HR-01's `RQN-2026-0106` is retitled with a new competency five-set and a re-drafted intake brief, HR-03's twelve interview files are re-drafted on that requisition with a peer program manager panel in place of the engineering one (four file renames), and HR-18 is regenerated behind the register, which moves one cell of `hris-requisitions.csv` and leaves the pack at 52 datasets and 25 drafted sets in the tag cut at its own merge, v1.13.0, after operations cluster 3's v1.12.0. The revenue cluster 4 slice (the drafted REV-02 account-fact-packets, three packets plus a 30-row fact index; the drafted REV-05 win-loss-call-note-corpus with its eight-deal registry; REV-08 campaign-segment-and-offer-brief, a deterministic five-variant segment filter computed from CORE-03 at build time plus its drafted offer brief) takes the pack to 56 datasets and 27 drafted sets (from operations cluster 3's 55) in the tag cut at its own merge, v1.14.0, after v1.12.0 (operations cluster 3) and v1.13.0 (people-hr Amendment A); that PR changes no byte under `datasets/core/`, `canon/`, the frozen cluster-2 and cluster-3 revenue directories, `artifacts/REV-04/` or `artifacts/REV-06/`, and its U-C4-T4 correction (P5-prime and T4-prime: the opposite-outcome pair lives in REV-05's own deal registry, the lost member joining the single Closed Lost opportunity and the won member witnessed by customer status plus a pre-export-window close date, because zero Closed Won opportunities exist in CORE-03) rides to the freeze review. People and HR cluster 2 (recruiting intake and screening) adds two drafted-frozen sets and no dataset at all, taking the pack to 56 datasets and 29 drafted sets in the tag cut at its own merge, expected v1.15.0 after revenue cluster 4's v1.14.0: HR-02 resume-corpus (eighteen resumes on one open requisition plus the recruiter's application log, whose screening vocabulary has no value meaning shortlisted or rejected, because the decision has not been made) and HR-04 interview-scorecards-question-banks (a 37-question bank with its protected-characteristic probe list and panel-role map, a weighted screening rubric, and six scorecards paired with the frozen HR-03 transcripts, which are where a panelist `employee_id` first appears). Revenue cluster 5, attribution and the deck, closes the revenue data build with two deterministic sets under `datasets/revenue/`: REV-09 play-trace-ground-truth (eight play traces enumerated by rule from the REV-03 triggers through the taught gates, plus three attribution claims of which exactly one cites a trace that does not exist) and REV-10 deck-and-proposal-source-pack (a 21-figure QBR numbers view in exact cents under the declared v1 stage weights, the repo's first binary dataset file, a stored-zip ECMA-376 deck template with fourteen named placeholder slots, its slot manifest emitted from the same layout definition, and the drafted approved-claims-and-proposal-language sheet under `artifacts/REV-10/`, whose single unapproved row is the corpus's both-directions procurement_process attribute), taking the pack to 58 datasets and 29 drafted sets in the tag cut at its own merge, expected v1.16.0; that PR changes no byte under `datasets/core/`, `canon/` or any frozen revenue or other-track directory, and its U-C5-T3 correction (T3-prime: attributed traces declare the influenced-existing basis and the sourced count is pinned at zero, because the export's latest opportunity created date, 2026-02-10, precedes the whole trigger window) rides to the freeze review. Check `MANIFEST.json` for what is actually present before assuming a dataset exists.
